h1>id: 
<%= @tasklists.id %>
 のメッセージ編集ページ</h1>

<%= form_for(@tasklists) do |f| %>

  
<%= f.label :content, 'メッセージ' %>

  
<%= f.text_field :content %>


  
<%= f.submit '投稿' %>

<% end %>


<%= link_to '一覧に戻る', tasklists_path %>